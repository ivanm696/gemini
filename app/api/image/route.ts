if (!GEMINI_API_KEY || typeof GEMINI_API_KEY !== 'string' || GEMINI_API_KEY.length < 10) { console.error('Invalid API key'); return NextResponse.json({ success: false, error: 'Invalid API key' }, { status: 500 }); }